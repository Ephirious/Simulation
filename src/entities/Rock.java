package entities;

import util.Coordinates;
import util.Sprite;

public class Rock extends Entity {
    public Rock(Coordinates coordinate, Sprite entitySprite) {
        super(coordinate, entitySprite);
    }
}
